"""
モニター機種別 Skill 定義
各 Skill の prompt_hint が Claude Vision への追加プロンプトとして使用される。
"""

# ──────────────────────────────────────────────────────────────
# 全 Skill 共通の重要注意事項
# ──────────────────────────────────────────────────────────────
_COMMON_CAUTIONS = """
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
■ 特に混同しやすい項目（必ず守ること）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【1. 血圧：NIBP（非観血）と ART/P1/P2（観血）の区別】
血圧には2種類の計測方法があり、モニターに両方表示されることがある。
- 非観血血圧（NIBP または BP）：カフで測定。間欠的に更新される。
- 観血血圧（ART、P1、P2）：動脈ラインで測定。リアルタイム更新。

読み取り時のルール：
- 画面に ART（観血血圧）が表示されている場合 → ART の値を優先して使用する
- ART が表示されていない場合 → NIBP/BP の値を使用する
- いずれも「---」「-?-」等の未測定表示の場合 → null
- 必ず notes フィールドに「血圧: ART使用」または「血圧: NIBP使用」と記録すること

【2. ISOダイヤル と ISO In/Et は別物】
- iso_dial（ISOダイヤル）
  → 気化器の設定ダイヤルの値。モニター画面には表示されない。
  → 必ず null を返すこと。ISO In/Et の値で代用しないこと。

- iso_inspired（ISO In）/ iso_expired（ISO Et または Ex）
  → 実際に測定された吸気・呼気中のイソフルラン濃度。モニター画面に表示される。
  → これらは iso_dial とは別の項目であり、正しく読み取ること。

【3. FiO2 と ガス流量 O2/Air は別物】
- fio2（FiO2）
  → 吸入酸素濃度（%）。モニター画面の「O₂」または「FiO₂」に表示される。
  → 画面から読み取り可能。

- gas_flow_o2 / gas_flow_air（ガス流量）
  → 流量計で設定している O2・Air の流量（L/min）。
  → モニター画面ではなく流量計・麻酔器設定部に表示されることが多い。
  → 画面上に明確な値が見えない場合は null を返すこと。FiO2 の値で代用しないこと。
"""

# ──────────────────────────────────────────────────────────────
# モニター選択肢（UI 表示用）
# ──────────────────────────────────────────────────────────────
MONITOR_OPTIONS = [
    {"id": "generic",         "label": "機種指定なし（汎用）"},
    {"id": "fukuda_am140",    "label": "フクダエム・イー Bio-Scope AM140（動物用）"},
    {"id": "drager_vista300", "label": "Dräger Vista 300 + 麻酔器（2画面）"},
]

# ──────────────────────────────────────────────────────────────
# Skill 定義辞書
# ──────────────────────────────────────────────────────────────
MONITOR_SKILLS: dict[str, dict] = {

    # ──────────────────────────────────────────────
    # Skill 1: フクダエム・イー工業 Bio-Scope AM140
    # ──────────────────────────────────────────────
    "fukuda_am140": {
        "id": "fukuda_am140",
        "name": "フクダエム・イー工業 Bio-Scope AM140（動物用生体モニター）",
        "prompt_hint": """
このモニターは「フクダエム・イー工業株式会社 動物用生体モニター Bio-Scope AM140」です。
以下の画面レイアウトと表示仕様を参考に、各項目を正確に読み取ってください。

■ 画面全体のレイアウト
- 黒背景の横長画面
- 左側 約65%：波形表示エリア（複数チャンネルが縦に並ぶ）
- 右側 約35%：デジタル数値表示エリア（大きな数字）
- 最下部：サマリーバー（小さな文字で主要値が横一列に並ぶ）

■ 波形チャンネル（上から順）
1. ECG 波形：緑色、鋭いRピーク
2. SpO2 プレチスモグラフィ波形：橙／黄色
3. 動脈圧または他の生体信号波形
4. カプノグラフィまたは呼吸波形

■ 各パラメータの読み取り方

【heart_rate（心拍数）】
- ラベル「HR」の数値を使用 [bpm]
- 「PR」（Pulse Rate / 脈拍数）はSpO2由来の別値であり heart_rate には使用しない
- 緑または青緑色の大きな数字で表示される

【spo2（SpO2）】
- ラベル「SpO₂」の数値 [%]
- 黄または橙色の大きな数字。直下に小数値（PI: 灌流指数）が表示される場合があるが spo2 ではない

【blood pressure（血圧3値）】
- 観血圧（ART, P1, P2）が表示されている場合 → その値を優先して使用する
  - bp_systolic：収縮期値 [mmHg]
  - bp_mean：平均圧値 [mmHg]
  - bp_diastolic：拡張期値 [mmHg]
- 観血圧がない場合 → ラベル「BP」の SYS / MAP / DIA を使用する
- 「---」「?」表示は未測定 → null
- notes に「血圧: ART使用」または「血圧: NIBP使用」と記録すること

【etco2（EtCO2）】
- ラベル「CO₂」の欄に「In」（吸気）と「ET」または「Et」（呼気）の2値が分離表示される
- etco2 には「ET」または「Et」の値のみを使用 [mmHg]
- 「In」側の値は etco2 に使用しない

【respiratory_rate（呼吸数）】
- ラベル「RR」の数値 [回/分]

【tidal_volume（1回換気量）】
- ラベル「VT」の数値 [mL]

【minute_ventilation（分時換気量）】
- ラベル「MV」の数値 [L/min]

【peak_airway_pressure（最高気道内圧）】
- 表示されている場合はラベル「Ppeak」「Pmax」または「Paw」の数値 [cmH2O]
- この画面では明確に表示されていない場合は null

【iso_inspired / iso_expired（ISO 吸気・呼気濃度）】
- ラベル「ISO」に「In」（吸気）と「Ex」または「Et」（呼気）の2値が表示される
  - iso_inspired：「In」の値 [%]
  - iso_expired：「Ex」または「Et」の値 [%]

【fio2（FiO2）】
- ラベル「O₂」または「FiO₂」の数値 [%]

【body_temperature（体温）】
- ラベル「TEMP」または「T1」「T2」の数値 [°C]
- 最下部サマリーバーに表示される「体温 37.5-38.5°C」等は参照範囲ラベルのため使用しないこと
- °F 表示の場合は °C に換算：(°F - 32) × 5/9

■ 最下部サマリーバーの補足
- 「NNN/NNN」形式 → 血圧 収縮期/拡張期
- 続く2〜3桁の数値 → 平均血圧
- 小数を含む数値（例: 38.3）→ 体温 [°C]
- 2桁の整数 → SpO2 [%]
- 「N.N/N.N」形式（例: 1.8/1.3）→ ISO In/Ex（吸気/呼気濃度 [%]）
""" + _COMMON_CAUTIONS,
    },

    # ──────────────────────────────────────────────────────────
    # Skill 2: Dräger Vista 300（患者モニター）＋ Dräger 麻酔器
    # ──────────────────────────────────────────────────────────
    "drager_vista300": {
        "id": "drager_vista300",
        "name": "Dräger Vista 300 + 麻酔器（2画面構成）",
        "prompt_hint": """
このモニターは「Dräger Vista 300（患者モニター）」と「Dräger 麻酔器」の2画面構成です。
画像には2台の画面が横に並んでいます。左画面と右画面それぞれから情報を読み取ってください。

━━━━━━━━━━━━━━━━━━━━━━
■ 左画面：Dräger Vista 300（患者モニター）
━━━━━━━━━━━━━━━━━━━━━━
【画面レイアウト】
- 黒背景。上部に「Dräger」ロゴと青いアクセントバー。画面下部に「Vista 300」の表示
- 左半分：波形表示エリア（ECG＝緑、SpO2プレチスモ＝黄／シアン等）
- 右半分：大きなデジタル数値エリア
- 最下部：ステータスバーと警告表示

【heart_rate（心拍数）】
- ラベル「HR」の数値を使用 [bpm]（ECG由来）
- ラベル「PR」（Pulse Rate / SpO2由来）は heart_rate に使用しない
- 緑色の大きな数字で表示される

【spo2（SpO2）】
- ラベル「SpO₂」の数値 [%]（シアンまたは黄色の大きな数字）
- 直下の小数値（例: 0.0）は PI（灌流指数）であり spo2 ではない

【blood pressure（血圧3値）】
- 表示形式：「収縮期/拡張期 (平均)」 例：「125/101 (109)」
  - bp_systolic：スラッシュ前の値（例: 125）[mmHg]
  - bp_diastolic：スラッシュ後・括弧前の値（例: 101）[mmHg]
  - bp_mean：括弧 ( ) 内の値（例: 109）[mmHg]
- ART（観血的動脈圧）と NIBP（非観血的血圧）の両方が表示されている場合は ART を優先
- 「-?-/-?-」「---/---」が表示されている場合 → null
- notes に「血圧: ART使用」または「血圧: NIBP使用」と記録すること

【body_temperature（体温）】
- ラベル「TEMP」または「T1」「T2」の橙色の数値 [°C]
- 画面最下部の「体温 37.5-38.5°C」等は参照範囲ラベルのため使用しないこと

━━━━━━━━━━━━━━━━━━━━━━
■ 右画面：Dräger 麻酔器
━━━━━━━━━━━━━━━━━━━━━━
【画面レイアウト】
- 黒背景。上部に「Dräger」ロゴ
- 上部：呼吸波形エリア（カプノグラフィ・気道圧波形等）
- 右側：数値パネル（呼吸・ガスパラメータ）
- 下部：バポライザーや流量計の表示エリア（円形アイコン）

【etco2（EtCO2）】
- 右画面の「CO₂」ラベルの大きな数値 [mmHg]（カプノグラフィ波形の隣）

【respiratory_rate（呼吸数）】
- 右画面の呼吸回数の数値（例: 11）[回/分]

【tidal_volume（1回換気量）】
- ラベル「VT」の数値 [mL]
- L 単位表示の場合（例: 0.72 L）は ×1000 して mL に換算（例: 720）

【minute_ventilation（分時換気量）】
- ラベル「MV」の数値 [L/min]（例: 4.4）

【peak_airway_pressure（最高気道内圧）】
- ラベル「Paw」「PIP」「Peak」または「Ppeak」の数値 [cmH2O]

【iso_inspired / iso_expired（ISO 吸気・呼気濃度）】
- 右画面の「Iso」ラベルに吸気・呼気の2値が表示される
  - iso_inspired：吸気側の値 [%]
  - iso_expired：呼気側の値 [%]

【fio2（FiO2）】
- 右画面の「O₂」または「FiO₂」ラベルの吸気側の数値 [%]
- O₂ 行に2値ある場合は吸気側を使用

【タイマー表示】
- 「0:10:00」等のタイマーはバイタル値ではないため抽出不要
""" + _COMMON_CAUTIONS,
    },
}
